image: maven:3-jdk-11-slim

cache:
  paths:
    - /root/.m2

build:
  stage: build
  script:
    - mvn compile

test:
  stage: test
  script:
    - mvn verify
  artifacts:
    reports:
      junit:
        - target/surefire-reports/TEST-*.xml
        - taqrget/dependency-check-report.html